#!/bin/sh
set -eu
if [ -n "${D:-}" ]; then
    exit 0
fi
mkdir -p /etc/enigma2/e2doctor/backups
chmod 700 /etc/enigma2/e2doctor /etc/enigma2/e2doctor/backups
# No GUI restart and no configuration overwrite during package installation.
exit 0
